# Core runtime dependencies
#
# The basic GAME framework currently uses only the Python standard library.
# This file is intentionally minimal so new students can run the template
# without installing a large AI stack.
#
# To connect real model providers, install:
#   pip install -r requirements-ai.txt
#
# For testing and development tools, install:
#   pip install -r requirements-dev.txt
